@model IEnumerable<m.Models.Leave>

@{
    ViewBag.Title = "Master Policy Sick Leaves";
}

<h2>Master Policy Sick Leaves</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.num_allowed_max)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.num_allowed_year)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.num_taken)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.num_allowed_max)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.num_allowed_year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.num_taken)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.ID }) |
            @Html.ActionLink("Details", "Details", new { id = item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ID })
            </td>
        </tr>
    }

</table>
